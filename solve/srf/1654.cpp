#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int k,n;
	cin>>k>>n;
	vector<int> s(k);
	while(k--){
		cin>>s[k];
	}
	int max_r =*max(s.begin(), s.end());
	int l=1;

	

	return 0;
}